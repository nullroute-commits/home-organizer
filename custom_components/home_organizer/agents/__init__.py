# -*- coding: utf-8 -*-
# // [v9.0.0 | 2026-04-13] Purpose: Package marker for agents. Each agent is a
# // self-contained module with its own prompt, run function, and tools. The
# // dispatcher imports them lazily so a broken agent cannot crash siblings.
